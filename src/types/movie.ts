export type Movie = {
  movieId: number;
  category: string;
  title: string;
  year: number;
  director: string;
  rating: number;
  edited: boolean;
  lentTo: string;
  notes: string;
};
